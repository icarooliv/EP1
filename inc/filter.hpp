#ifndef FILTER_HPP
#define FILTER_HPP

#include <string>

using namespace std;

class Filter {
protected:
   Filter();  // Metodo Construtor
   ~Filter(); // Metodo Destrutor

   void setApply();
};

#endif
